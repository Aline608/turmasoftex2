// Número
let numero: number = 42;

// Nulo (null)
let nulo: null = null;

// String
let texto: string = "Olá, mundo!";

// Booleano
let booleano: boolean = true;

// Array
let arrayNumeros: number[] = [1, 2, 3, 4, 5];

// Objeto
let objeto: { chave1: string, chave2: number } = { chave1: "valor1", chave2: 42 };
